回归:
		1.指针  就是地址 来源于地址总线 
			两个操作：
					char c = 'a';
					&c; //类型：char \*  值：4/8
					char \* p = &c;
					//取到地址值最小的
				解引用：
						\*P ==>  C空间
						（char\*) ==>char
									char解引用得到char
			2.数组
			指针类型： 将当前数组作为一堆数据来看待，其所谓的第一个元素的地址 long long \*
				如果你要取arr里面的值的时候，当指针类型使用，其他都是当数组类型使用。
				![[1698975287925.png]]
#如果要去到字节里面的数据位，可以进行移位操作
![[1698976110123.png]]

题目![[1698977821391.png]]

内部+1，跳过一个类型，arr的类型是long long [8]所以跳过64位到了顶上
-4，然后强制类型转换-4，变成减少四个longlong类型，一个是八位，32位置，就到5.

=========================================
求第六个字节的第五位
![[1698978364855.png]]

![[1698978782550.png]]
++ref是错误的
++ref =
ref = ref+1
是错误的
ref是不允许发生改变的
